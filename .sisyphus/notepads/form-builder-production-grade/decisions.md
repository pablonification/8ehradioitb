
- [2026-02-24 12:25:32] Keep existing field behavior unchanged while centralizing by exporting `ALLOWED_FIELD_TYPES`, `FIELD_DEFAULTS`, and `FIELD_VALIDATORS`; use validator metadata (`requiresOptions`, `answerKind`) in contracts/submission validation and defaults in UI rendering/builder normalization.
