- [2026-02-24 12:25:32] Centralize event/profile field-type source of truth in `lib/events/formFieldConfig.js` and consume from UI + server contracts to prevent drift between builder, register, profile, and API validation.
- Form Builder Redesign: Implemented a modern builder layout with a top utility bar, dominant central canvas for questions, and a right rail for settings and responses. Used Tailwind CSS for elevation, spacing, and rounded sections to improve visual hierarchy.
- Form builder UI redesign: use stone/warm palette for softer feel, rounded-2xl for cards, gradient icon badges for section headers, backdrop-blur for glass morphism effect
- [2026-02-24] Form builder visual refinement: Removed decorative elements (gradient backgrounds, noise texture, glass morphism, gradient buttons, decorative lines) in favor of flat neutral colors (bg-stone-100 page, solid bg-stone-800 icon badges, solid bg-emerald-600 buttons). Cards now use simple shadow-sm + border-stone-200 for a cleaner SaaS builder aesthetic.

- [2026-02-24] Google Forms-like ergonomics: Redesigned question card header with solid bg-stone-800 position badge, field type badge, and 'Wajib' indicator. Grouped reorder actions (move-to-top/up/down/bottom using FiChevronsUp/Down) and edit actions (Duplikat/Hapus with visible labels). All buttons have aria-label, title, and focus:ring-2 for keyboard accessibility.
- [2026-02-24] Section dividers: Use `fieldType: '__divider__'` sentinel value filtered out in validateDraftPayload before both validation and normalization. Dividers are editor-only and excluded from server payload while allowing visual organization in the builder.
- [2026-02-24] Question numbering excluding dividers: Use `draftQuestions.slice(0, index + 1).filter(q => normalizeString(q.fieldType) !== '__divider__').length` to show correct position numbers that skip divider entries.
- [2026-02-24] Responses UX: Replaced dense table with card-based expandable list. Each response shows avatar initial, name/email, relative time badge, and answer preview chips (max 3 with "+N more" indicator). Expanded details show formatted key-value pairs with nested JSON debug toggle. Added distinct loading skeleton, empty state with contextual message, and error state with retry button. Search shows filtered count ("X dari Y response").
- [2026-02-24 16:10:00] Regression tests should assert rejection side effects for contract failures (`eventSubmission.create` not called) and preserve legacy success behavior when `formVersionId` is omitted for non-file forms.
- [2026-02-24 17:16:13] Production hardening: use structured `console.error` metadata (`operation`, contextual IDs, safe `errorMessage`) in API catch paths and pair frontend failures with explicit retry guidance so users have a clear recovery step.
  2026-02-24T00:00:00Z Added first-class file field contracts and strict submission metadata validation (key/name/mime strings + size > 0) to reject URL string answers safely.
- 2026-02-24T10:30:12Z Event upload API must issue server-generated keys scoped to events/<eventSlug>/users/<userId>/<source>/<fieldKey>/... with presigned URL TTL capped at 120 seconds and strict payload validation before auth/db work.
  2026-02-24T10:37:03Z - Integrated file field upload in shared FieldRenderer with event-scoped signed URL + PUT flow, recoverable retry/remove, and metadata answer persistence across register/profile.
- [2026-02-24T10:43:20Z] Final audit: file-field path is implemented end-to-end, but submission validation still does not enforce uploaded `answers.<file>.key` ownership/scope (event/user/source/field), and `isMissingProfileValue` still checks `file.url` instead of file metadata (`key/name/mime/size`), creating a completeness-contract mismatch risk.
- [2026-02-24 17:50:28] Align profile/register identity UX by reusing `AccountReveal` in `app/profile/page.jsx` so account visibility, masking behavior, and mobile spacing stay consistent without touching save/redirect logic.
- [2026-02-24 18:32:00] Keep `AccountReveal` wrapper padding consistent with register flow by using `className="flex-1"` in profile header; this avoids double-inset spacing while preserving hidden-by-default identity reveal and existing save/redirect behavior.
- [2026-02-24 18:05:00] Keep required file-field completeness checks identical across register/profile client validators and submission API contracts; otherwise users can pass UI validation but fail server-side on metadata shape (`key/name/mime/size`).
- [2026-02-24 18:05:00] Scope-fidelity gates should treat unresolved governance artifacts (for example missing F2 evidence) as release-signoff blockers even when implementation code mostly matches plan boundaries.
- [2026-02-24 18:18:00] Re-running governance after fixes prevents stale blockers: placeholder guardrails and evidence-completeness checks can flip scope fidelity from REJECT to ACCEPT without touching product code.
- [2026-02-24 18:12:00] Final compliance audit: most scope is implemented with concrete code/test coverage, but Task 11 remains a blocker because builder reorder currently uses button controls only and lacks drag-and-drop interaction.
- [2026-02-24 18:24:00] Re-audit after latest updates: Task 11 now satisfies plan intent with implemented drag-and-drop handlers and preserved keyboard reorder fallback in `app/dashboard/form/page.jsx`.
- [2026-02-24 11:12:00] F3 QA gate: unauthenticated checks confirm participant shell (no navbar), account reveal visibility, and mobile no-overflow, but full register/profile/dashboard interactions require authenticated test credentials; capture this precondition explicitly in release-gate QA plans.
- [2026-02-24 19:05:00] Implement native builder drag-and-drop by attaching HTML5 drag handlers to dedicated card handles (plus touch move/end fallback on `data-dnd-index` targets), so reorder works for both questions and `__divider__` entries without changing existing button-based keyboard controls or save payload normalization.
- [2026-02-24 19:20:00] Replace generic participant/profile placeholder copy with field-type-aware helper text (email/phone/url/date/number/textarea/default) so guidance stays contextual without changing empty default answer behavior.
